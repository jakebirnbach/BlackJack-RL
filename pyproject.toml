[project]
name = "blackjack-rl"
version = "0.1.0"
description = "Reinforcement learning agent for blackjack"
requires-python = ">=3.13"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
